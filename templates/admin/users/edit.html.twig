{% extends 'base.html.twig' %}

{% block title %}
    Dashboard - Edition - {{ parent() }}
{% endblock title %}

{% block body  %}
    
    {% block header %}
        {{ parent() }}
    {% endblock header %}
<div class="container">
    <h1>Formulaire de modification d'un membre étudiant</h1>
    <hr>

    {{ include('users/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('users_index') }}">back to list</a>

    {{ include('users/_delete_form.html.twig') }}
</div>
{% endblock %}
